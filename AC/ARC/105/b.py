from typing import List


def calcMin(arr: List[int]) -> int:
    mmax = max(arr)
    mmin = min(arr)
    max % min


n = int(input())
a = list(map(int, input().split()))
aset = set(a)

X = max(aset)
x = min(aset)
if X % x == 0:
