package main

import (
	"bufio"
	"fmt"
	"os"
	"strings"
)

func main() {
	// input
	var n, k int
	fmt.Scan(&n, &k)
	//sc := bufio.NewScanner(os.Stdin)
	//sc.Scan()
	//r := strings.Split(sc.Text(), " ")
}
